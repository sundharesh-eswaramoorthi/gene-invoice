package com.geneinvoice.strategy;

/** Selectable invoice-total-amount predicate operators for a notification strategy. */
public enum AmountOperator {
    LESS_THAN,
    EQUAL,
    GREATER_THAN,
    BETWEEN
}
